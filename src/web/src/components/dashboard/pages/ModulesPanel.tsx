import { moduleCategories } from "../core/moduleData";
import type { BotModule } from "../core/types";
import { Icon, Toggle } from "../core/UI";

export default function ModulesPanel({ modules, onToggle }: { modules: BotModule[]; onToggle: (id: string) => void }) {
  return <><section className="notice"><span>✦</span><p>Các thay đổi được áp dụng ngay lập tức. Một vài module có thể cần quyền riêng để hoạt động đầy đủ.</p></section><div style={{ display: "grid", gap: 30 }}>{moduleCategories.map((category) => <section key={category.title}><div style={{ display: "flex", alignItems: "center", gap: 10, margin: "0 0 12px 3px" }}><span style={{ width: 29, height: 29, display: "grid", placeItems: "center", color: "#c0aeff", borderRadius: 8, background: "#7659df25", fontSize: 14 }}>{category.icon}</span><div><h2 style={{ margin: 0, color: "#efeff6", fontSize: 14, fontFamily: "Plus Jakarta Sans" }}>{category.title}</h2><p style={{ margin: "3px 0 0", color: "#858798", fontSize: 10 }}>{category.description}</p></div></div><div className="module-cards" style={{ gridTemplateColumns: "repeat(auto-fit, minmax(230px, 1fr))" }}>{modules.filter((module) => category.moduleIds.includes(module.id)).map((module) => <article className="module-card" key={module.id}><div className="module-card-top"><div className={`module-icon large ${module.tone}`}>{module.icon}</div><Toggle checked={module.enabled} onClick={() => onToggle(module.id)} label={`Bật tắt ${module.name}`} /></div><h3>{module.name}</h3><p>{module.description}</p><div className="module-card-footer"><span className={module.enabled ? "enabled-label" : "disabled-label"}><i /> {module.enabled ? "Đang hoạt động" : "Đang tạm dừng"}</span><button>Thiết lập <Icon name="arrow" /></button></div></article>)}</div></section>)}</div></>;
}
